---
title: "Set up a Cisco Ise connector"
og:title: "Set up a Cisco Ise connector"
description: "C1 provides identity governance and just-in-time provisioning for Cisco Ise. Integrate your Cisco Ise instance with C1 to run user access reviews (UARs), enable just-in-time access requests, and automatically provision and deprovision access."
og:description: "C1 provides identity governance and just-in-time provisioning for Cisco Ise. Integrate your Cisco Ise instance with C1 to run user access reviews (UARs), enable just-in-time access requests, and automatically provision and deprovision access."
sidebarTitle: "Cisco Ise"
---

{/* AUTO-GENERATED:START - capabilities
     Generated from baton_capabilities.json. Do not edit manually. */}

## Capabilities

The Cisco Ise connector syncs the following resources:

| Resource | Sync | Provision |
| :--- | :--- | :--- |
| Accounts | <Icon icon="square-check" iconType="solid" color="#65DE23"/> | <Icon icon="square-check" iconType="solid" color="#65DE23"/> |
| Admin Users | <Icon icon="square-check" iconType="solid" color="#65DE23"/> | |
| Identity Groups | <Icon icon="square-check" iconType="solid" color="#65DE23"/> | <Icon icon="square-check" iconType="solid" color="#65DE23"/> |

{/* AUTO-GENERATED:END - capabilities */}

<Note>
  Admin user syncing is opt-in. Reading admin users requires an ERS account with the admin-user read scope, which not every ISE deployment grants. Enable the Admin Users resource only when your ERS account has that scope; otherwise leave it disabled and the connector will not attempt the sync.
</Note>

## Gather Cisco Ise credentials

<Warning>
To configure the Cisco ISE connector, you need an administrator account with ERS (External RESTful Services) API access enabled.
</Warning>

<Steps>
  <Step>
    Sign in to the Cisco ISE admin console as a Super Admin.
  </Step>

  <Step>
    Enable ERS:

    Navigate to **Administration** > **System** > **Settings** > **API Settings** > **API Service Settings** and enable **ERS (Read/Write)**.
  </Step>

  <Step>
    Create or designate an admin user with the **ERS Admin** role:

    Go to **Administration** > **System** > **Admin Access** > **Administrators** > **Admin Users**, and either create a new user or assign the **ERS Admin** role to an existing one.
  </Step>

  <Step>
    Note the ISE hostname (e.g., `ise.example.com`), the admin username, and password. These are the credentials ConductorOne will use.
  </Step>
</Steps>

## Configure the Cisco Ise connector

<Tabs>
  <Tab title="Cloud-hosted">
    Follow these instructions to use a built-in, no-code connector hosted by C1.

    <Steps>
      <Step>
        In C1, navigate to **Integrations** > **Connectors** and click **Add connector**.
      </Step>

      <Step>
        Search for **Cisco Ise** and click **Add**.
      </Step>

      <Step>
        Choose how to set up the new Cisco Ise connector:

        - Add the connector to a currently unmanaged app
        - Add the connector to a managed app
        - Create a new managed app
      </Step>

      <Step>
        Set the owner for this connector.
      </Step>

      <Step>
        Click **Next**.
      </Step>

      <Step>
        Find the **Settings** area of the page and click **Edit**.
      </Step>

{/* AUTO-GENERATED:START - config-params
     Generated from config_schema.json. Do not edit manually. */}

      <Step>
        Enter the required configuration:

        - **Hostname**: Your Cisco ISE hostname (e.g., `ise.example.com`)
        - **Username**: ERS API administrator username
        - **Password**: ERS API administrator password
      </Step>

{/* AUTO-GENERATED:END - config-params */}

      <Step>
        Click **Save**.
      </Step>

      <Step>
        The connector's label changes to **Syncing**, followed by **Connected**. You can view the logs to ensure that information is syncing.
      </Step>
    </Steps>

    **That's it!** Your Cisco Ise connector is now pulling access data into C1.
  </Tab>

  <Tab title="Self-hosted">
    Follow these instructions to use the [Cisco Ise](https://github.com/conductorone/baton-cisco-ise) connector, hosted and run in your own environment.

    When running in service mode on Kubernetes, a self-hosted connector maintains an ongoing connection with C1, automatically syncing and uploading data at regular intervals.

    
    ### Resources

    * [Official download center](https://dist.conductorone.com/ConductorOne/baton-cisco-ise): For stable binaries (Windows/Linux/macOS) and container images.

    * [GitHub repository](https://github.com/conductorone/baton-cisco-ise): Access the source code, report issues, or contribute to the project.

    ### Step 1: Set up a new Cisco Ise connector

    <Steps>
      <Step>
        In C1, navigate to **Integrations** > **Connectors** > **Add connector**.
      </Step>

      <Step>
        Search for **Baton** and click **Add**.
      </Step>

      <Step>
        Choose how to set up the new Cisco Ise connector:

        - Add the connector to a currently unmanaged app
        - Add the connector to a managed app
        - Create a new managed app
      </Step>

      <Step>
        Set the owner for this connector.
      </Step>

      <Step>
        Click **Next**.
      </Step>

      <Step>
        In the **Settings** area of the page, click **Edit**.
      </Step>

      <Step>
        Click **Rotate** to generate a new Client ID and Secret.

        Carefully copy and save these credentials.
      </Step>
    </Steps>

    ### Step 2: Create Kubernetes configuration files

    Create two Kubernetes manifest files for your Cisco Ise connector deployment:

    #### Secrets configuration

    ```yaml expandable
    # baton-cisco-ise-secrets.yaml
    apiVersion: v1
    kind: Secret
    metadata:
      name: baton-cisco-ise-secrets
    type: Opaque
    stringData:
      # C1 credentials
      BATON_CLIENT_ID: <C1 client ID>
      BATON_CLIENT_SECRET: <C1 client secret>

      # Cisco Ise credentials
      # Add connector-specific credentials here
    ```

    See the connector's README or run `--help` to see all available configuration flags and environment variables.

    #### Deployment configuration

    ```yaml expandable
    # baton-cisco-ise.yaml
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: baton-cisco-ise
      labels:
        app: baton-cisco-ise
    spec:
      selector:
        matchLabels:
          app: baton-cisco-ise
      template:
        metadata:
          labels:
            app: baton-cisco-ise
            baton: "true"
            baton-app: cisco-ise
        spec:
          containers:
          - name: baton-cisco-ise
            image: public.ecr.aws/conductorone/baton-cisco-ise:latest
            imagePullPolicy: IfNotPresent
            env:
            - name: BATON_HOST_ID
              value: baton-cisco-ise
            envFrom:
            - secretRef:
                name: baton-cisco-ise-secrets
    ```

    ### Step 3: Deploy the connector

    <Steps>
      <Step>
        Create a namespace in which to run C1 connectors (if desired), then apply the secret config and deployment config files.
      </Step>

      <Step>
        Check that the connector data uploaded correctly. In C1, click **Applications**. On the **Managed apps** tab, locate and click the name of the application you added the Cisco Ise connector to. Cisco Ise data should be found on the **Entitlements** and **Accounts** tabs.
      </Step>
    </Steps>

    **That's it!** Your Cisco Ise connector is now pulling access data into C1.
  </Tab>
</Tabs>

---

<Tip>
All versions of this connector are available at [dist.conductorone.com](https://dist.conductorone.com/ConductorOne/baton-cisco-ise).
</Tip>