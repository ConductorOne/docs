---
title: "Set up a Twist connector"
og:title: "Set up a Twist connector"
description: "C1 provides identity governance for Twist. Integrate your Twist workspace with C1 for unified visibility and governance over user access."
og:description: "C1 provides identity governance for Twist. Integrate your Twist workspace with C1 for unified visibility and governance over user access."
sidebarTitle: "Twist"
---

C1 provides identity governance for Twist. Integrate your Twist workspace
with C1 for unified visibility and governance over user access.

## Capabilities

|Resource|Sync|Provision|
|-|-|-|
|Users|<Icon icon="square-check" iconType="solid" color="#c937ae"/>||
|Groups|<Icon icon="square-check" iconType="solid" color="#c937ae"/>||

The connector reads one Twist workspace — the workspace whose numeric ID
you configure. It syncs that workspace's users and groups, and the
membership of each group. Removed (deactivated) members appear in the sync
as disabled users, which is useful for offboarding review. Guests sync
alongside regular members and admins, with the member type carried on the
user profile. Bot accounts sync as service accounts. Members who have been
invited but have not yet joined sync as enabled users with a pending flag
on their profile.

## Gather Twist credentials

<Steps>
  <Step>
    Sign in to your Twist workspace.
  </Step>

  <Step>
    Open **Manage integrations** and add a new integration (or open an
    existing one you control).
  </Step>

  <Step>
    In the integration's **OAuth** section, copy the test token. The test
    token authorizes as your own account with full access — no OAuth
    authorization flow is needed.
  </Step>

  <Step>
    Note your workspace's numeric ID. It appears in the workspace's URL on
    twist.com.
  </Step>
</Steps>

<Note>
A token can reach every workspace its user belongs to. The connector only
reads the workspace whose ID you configure, and checks at setup time that
the token can reach it.
</Note>

## Configure the Twist connector

<Tabs>
  <Tab title="Cloud-hosted">
    Follow these instructions to use a built-in, no-code connector hosted by C1.

    <Steps>
      <Step>
        In C1, navigate to **Integrations** > **Connectors** and click **Add connector**.
      </Step>

      <Step>
        Search for **Twist** and click **Add**.
      </Step>

      <Step>
        Choose how to set up the new Twist connector.
      </Step>

      <Step>
        Set the owner for this connector.
      </Step>

      <Step>
        Click **Next**.
      </Step>

      <Step>
        Find the **Settings** area of the page and click **Edit**.
      </Step>

      <Step>
        Enter the Twist credentials:

        - **Twist API base URL**: the Twist API origin, with no trailing
          slash. For Twist's hosted service this is
          `https://api.twist.com`.
        - **Twist API token**: the token you copied from the
          integration's OAuth section.
        - **Twist workspace ID**: the numeric ID of the workspace to
          sync.
      </Step>

      <Step>
        Click **Save**.
      </Step>

      <Step>
        The connector's label changes to **Syncing**, followed by **Connected**. You can view the logs to ensure that information is syncing.
      </Step>
    </Steps>

    **Done.** Your Twist connector is now pulling access data into C1.
  </Tab>

  <Tab title="Self-hosted">
    Follow these instructions to run the Twist connector in your own
    environment.

    <Steps>
      <Step>
        Create a secret for the Twist API token.
      </Step>

      <Step>
        Configure the connector environment variables:

        - **BATON_BASE_URL**: the Twist API origin, with no trailing
          slash. For Twist's hosted service this is
          `https://api.twist.com`.
        - **BATON_TWIST_API_TOKEN**: the token you copied from the
          integration's OAuth section.
        - **BATON_TWIST_WORKSPACE_ID**: the numeric ID of the workspace
          to sync.
      </Step>

      <Step>
        Deploy the connector using your standard self-hosted connector process.
      </Step>
    </Steps>

    **Done.** Your Twist connector is now pulling access data into C1.
  </Tab>
</Tabs>
